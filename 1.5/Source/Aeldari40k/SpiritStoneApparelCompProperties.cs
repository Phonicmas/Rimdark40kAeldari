using Verse;


namespace Aeldari40k
{
    public class SpiritStoneApparelCompProperties : CompProperties
    {
        public SpiritStoneApparelCompProperties()
        {
            compClass = typeof(SpiritStoneApparelComp);
        }
    }
}